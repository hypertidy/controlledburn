% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crop_burn.R
\name{crop_burn}
\alias{crop_burn}
\title{Crop a controlledburn result to a target extent}
\usage{
crop_burn(x, target)
}
\arguments{
\item{x}{A \code{controlledburn} object returned by \code{\link[=burn]{burn()}}.}

\item{target}{numeric vector \code{c(xmin, xmax, ymin, ymax)} defining
the target window. Snapped outward to cell boundaries.}
}
\value{
A \code{controlledburn} object covering only the target window,
with row/col indices re-based to 1.
}
\description{
Filters and clips the sparse tables (runs, edges, lines, points)
to a sub-window defined by a target extent. Returns a new
\code{controlledburn} object with offset row/col indices and snapped
extent. No dense allocation — just data frame filtering.
}
\examples{
if (requireNamespace("geos", quietly = TRUE)) {
  library(geos)
  poly <- as_geos_geometry("POLYGON ((1 1, 9 1, 9 9, 1 9, 1 1))")
  r <- burn(poly, extent = c(0, 10, 0, 10), dimension = c(100L, 100L))
  sub <- crop_burn(r, target = c(2, 5, 3, 7))
  sub
}
}
