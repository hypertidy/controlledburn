% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_burn.R
\name{extract_burn}
\alias{extract_burn}
\title{Extract values from a controlledburn result at point locations}
\usage{
extract_burn(x, xy, fun = c("id", "sum"))
}
\arguments{
\item{x}{A \code{controlledburn} object returned by \code{\link[=burn]{burn()}}.}

\item{xy}{A two-column matrix or data.frame of x, y coordinates in
the CRS of the burn.}

\item{fun}{character, \code{"id"} (last geometry written wins, \code{NA}
background) or \code{"sum"} (accumulated coverage/length/count, \code{0}
background). Same semantics as \code{\link[=materialize_chunk]{materialize_chunk()}}.}
}
\value{
A numeric vector of length \code{nrow(xy)}. Points outside the
grid extent are \code{NA} for both funs.
}
\description{
Queries the sparse tables directly; no dense matrix is created.
For points at cell centres this reproduces \code{\link[=materialize_chunk]{materialize_chunk()}}
exactly:
\code{extract_burn(x, xy, fun) == materialize_chunk(x, fun)[cbind(row, col)]}
}
\details{
Interior runs are matched by row equality plus column interval
containment; edges, lines, and points by exact (row, col) match.
Cost is O(points + hits), independent of grid dimension.
}
